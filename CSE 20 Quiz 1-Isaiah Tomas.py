#The below program adds together two floating-point (real) numbers. Adapt the program so that x and y take in integers instead. 
#Next, add code to output the type of the sum of the two inputs.

#Inputs - let's ask the user to input two numbers
x = int(input("Please enter a number: "))
y = int(input("Please enter another number: "))

z = x + y # Add the inputs together
2
#Outputs
#Let's print the result
print("The sum of your two inputs is:", z)
print("The type of your two inputs is:", type(z))
print("\n")

#Add code to output the 'type' of the sum of the two inputs